// src/hooks/index.ts

// Auth hooks (React Query + Zustand)
export * from './auth';

// Chat hooks (React Query + Zustand + WSE)
export * from './chat';

// Company hooks (React Query + WSE)
export * from './company';

// Telegram hooks (React Query + WSE)
export * from './telegram';

// Admin hooks (React Query + WSE)
export * from './admin';

// Supergroups hook
export * from './useSupergroups';

// OAuth (if still needed)
export * from './useOauth';
